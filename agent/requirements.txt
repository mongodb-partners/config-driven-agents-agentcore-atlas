# Upper bounds are deliberate. A workshop stack that was built and verified on
# one day must build identically on the day it is taught — an unpinned major
# release is a live-demo failure with no time to debug it.
bedrock-agentcore>=0.1.0,<1
strands-agents>=1.0.0,<2
# mcp 2.0.0 renamed streamablehttp_client -> streamable_http_client, which breaks
# mongo_mcp.py's import outright.
mcp>=1.9.0,<2
boto3>=1.40.0
httpx>=0.27.0
PyYAML>=6.0
# AgentCore's built-in observability: auto-instruments the process so spans and
# logs land in CloudWatch / X-Ray without any exporter code of ours.
aws-opentelemetry-distro>=0.10.0
